# Frontend (React)

## Setup
1. Copy `.env.example` to `frontend/.env` (or put env vars in your shell).
2. Install:
   ```
   cd frontend
   npm install
   npm start
   ```
3. The app expects Firebase to be configured. See firebaseConfig.js and populate env variables.

